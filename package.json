{
  "name": "render-node-app",
  "version": "1.0.0",
  "description": "A basic Node.js and Express server deployed on Render.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.18.2"
    // IMPORTANT: If your server.js uses other modules (like dotenv, body-parser, mysql),
    // you must add them here with their respective versions.
  }
}
